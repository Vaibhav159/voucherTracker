---
trigger: always_on
---

1. Build feature such that it works on light mode and dark mode
2. Build feature such that it works in web version and mobile browser.
3. Our site supports backend + static data(as a fallback), so always update static data first and make backend compatible with it.
